spring:
  application:
    name: hhplus
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:lecture;MODE=MySQL
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true